<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>


    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="5dp">


        <ImageView
            android:id="@+id/iv_space_detail_tag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/round_rectangle"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tv_space_detail_tag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginVertical="3dp"
            app:layout_constraintBottom_toBottomOf="@id/iv_space_detail_tag"
            app:layout_constraintEnd_toEndOf="@id/iv_space_detail_tag"
            app:layout_constraintStart_toStartOf="@id/iv_space_detail_tag"
            app:layout_constraintTop_toTopOf="@id/iv_space_detail_tag"
            tools:text="#태그"
            tools:textColor="@color/white"
            tools:textSize="8sp" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>